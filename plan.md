# RAG小论文快速产出方案（单跳/多跳自适应RAG）

## 一、论文定位与题目建议

### 核心定位

不追求颠覆性创新，而是做一个"务实、完整、有针对性"的 RAG 优化工作：

- 基线：标准 Dense Retrieval + LLM
- 增强：查询扩展 + 混合检索 + 重排序
- 微创新：**查询复杂度自适应策略**
- 实验亮点：在单跳数据集和多跳数据集上分别验证，分析不同查询复杂度下各模块的增益差异

### 可选题目

| 方向 | 题目 |
|---|---|
| 自适应路由 | 《面向单跳与多跳场景的查询复杂度自适应检索增强生成方法》 |
| 混合检索增强 | 《融合查询扩展与多路召回的自适应 RAG 框架及其在单跳/多跳问答中的验证》 |
| 动态策略 | 《基于查询类型感知的动态检索增强生成方法》 |

建议主标题里出现"自适应"和"单跳/多跳"，让论文一眼看出贡献点。

## 二、GPU 资源分配方案

两张 4090 24G，共 48G，建议如下分配：

| 模型 | 用途 | 显存占用 | 说明 |
|---|---:|---:|---|
| Qwen2.5-7B-Instruct / Llama-3.1-8B-Instruct（4bit 量化） | 生成答案 | 6—10 GB | 一张卡放 LLM，另一张卡放检索模型 |
| bge-large-zh-v1.5 / bge-m3 | 稠密检索 Embedding | 1.5—3 GB | 本地 embedding 推理 |
| bge-reranker-large / bge-reranker-v2-m3 | 重排序 | 2—4 GB | 本地重排 |
| BERT-base / DeBERTa-small | 查询复杂度分类器（微创新） | 1—3 GB | 训练一个小分类器 |
| 剩余显存 | 缓存、批处理、BM25 索引 | — | 足够 |

**部署方式：**

- LLM 用 `vLLM` 或 `llama.cpp` 部署，4bit 量化后生成速度很快。
- Embedding 和 Reranker 用 `sentence-transformers` 直接加载，本地推理。
- 查询复杂度分类器用 `transformers` 训练，几百 MB 模型，训练时间约 1—2 小时。

这样不需要调用 API，实验可复现，审稿人也会认可"全本地可复现"。

## 三、数据集选择：单跳 + 多跳

### 英文数据集（推荐主实验）

| 数据集 | 类型 | 说明 |
|---|---:|---|
| Natural Questions（NQ） | 单跳 | 标准开放域单跳问答，数据规范 |
| HotpotQA | 多跳 | 多跳推理，需要多个文档联合推理 |
| 2WikiMultihopQA | 多跳 | 更复杂的多跳问答，可选 |

建议：**NQ + HotpotQA** 作为主实验，如果时间够再加 2WikiMultihopQA 增强多跳说服力。

### 中文数据集（可选扩展实验）

| 数据集 | 类型 | 说明 |
|---|---:|---|
| DuReader | 单跳/多跳混合 | 中文阅读理解 |
| WebQA | 多跳 | 中文多跳问答 |

中文数据集可以放在"跨语言泛化实验"，但时间紧的话可以不做。

## 四、技术路线与实验设计

### 1. Baseline（G1）

```
query
  ↓
Dense Retriever（bge-large 嵌入 + FAISS）
  ↓
Top-5 文档片段
  ↓
拼接到 Prompt
  ↓
Qwen2.5-7B 生成答案
```

在 NQ 和 HotpotQA 上分别跑通，记录指标：

- EM
- F1
- Hit@5 / Recall@5
- RAGAS 的 Answer Relevancy / Faithfulness

### 2. 模块封装与消融

| 组别 | 方法 | 单跳（NQ） | 多跳（HotpotQA） |
|---|---|---|---|
| G1 | Baseline：Dense + LLM | 记录 | 记录 |
| G2 | Baseline + HyDE | 记录 | 记录 |
| G3 | Baseline + Hybrid Retrieval（BM25+Dense+RRF） | 记录 | 记录 |
| G4 | Baseline + Reranker | 记录 | 记录 |
| G5 | Baseline + HyDE + Hybrid Retrieval + Reranker | 记录 | 记录 |
| G6 | Full：G5 + 自适应查询策略（微创新） | 记录 | 记录 |

**关键点：每个模块分别在单跳和多跳上测试，观察增益差异。**

一般会看到：

- HyDE 在多跳上提升更明显，因为多跳问题需要更多语义扩展。
- Hybrid Retrieval 在多跳上提升更大，因为多跳需要精确词匹配。
- Reranker 在单跳上可能作用较小，在多跳上作用较大。
- 单跳基线已经不错，提升空间小；多跳提升空间大。

这些差异就是论文分析部分的素材。

### 3. 微创新：查询复杂度自适应策略

做一个轻量级查询复杂度分类器，将查询分为：

- **单跳简单查询**：直接 Dense Retrieval，Top-3，不重排。
- **多跳复杂查询**：HyDE + Hybrid Retrieval，Top-10，重排。
- **比较/列表类查询**：Hybrid Retrieval + Reranker + Query Decomposition。

#### 分类器设计

| 方案 | 优点 | 实现难度 |
|---|---|---|
| 基于规则 | 简单快速，无需训练 | 低 |
| BERT 文本分类 | 准确率高，可解释 | 中 |
| LLM 提示判断 | 灵活，但耗时 | 中 |

建议：**规则 + BERT 分类器混合**。

规则判断：

- 问题长度 > 12 个词 → 复杂
- 包含"and""which""compare""between"等词 → 复杂
- 包含多个实体 → 复杂

BERT 分类器：

- 用 HotpotQA 的"type"标签（comparison, bridge, intersection 等）和 NQ 的单跳标签训练。
- 输入问题，输出"single-hop"或"multi-hop"。
- 模型很小，两张 4090 上训练几分钟到几十分钟。

#### 自适应策略实现

```
query
  ↓
复杂度分类器
  ├── single-hop → Dense Retriever（Top-3）→ 直接生成
  └── multi-hop  → HyDE + Hybrid Retrieval（Top-10）→ Reranker → 生成
```

论文的核心创新点可以写成：

> 提出一种查询复杂度感知的自适应检索增强生成框架，通过轻量级分类器将查询分为单跳与多跳类型，并为其分配差异化的检索与重排序策略，有效平衡了单跳场景下的效率与多跳场景下的检索完整性。

## 五、实验数据可视化与写作

### 必须出的图

| 图 | 内容 |
|---|---|
| 框架图 | 展示自适应 RAG 流程，包含分类器、混合检索、重排序 |
| 主实验柱状图 | G1—G6 在 NQ 和 HotpotQA 上的 EM/F1 |
| 消融实验表 | 每个模块在单跳/多跳上的增量 |
| 单跳 vs 多跳差异图 | 同一方法在两个数据集上的提升幅度对比 |
| 效率对比图 | 单跳场景下自适应策略比固定复杂策略的 Token 数/时延降低 |
| 案例分析 | 单跳问题（简单）与多跳问题（复杂）的检索差异 |

### 论文写作大纲

1. **摘要**
   - 背景：RAG 在知识密集型任务中表现优异，但固定检索策略难以同时适配单跳与多跳问题。
   - 方法：提出查询复杂度自适应 RAG 框架，包含查询扩展、混合检索、重排序与复杂度路由。
   - 结果：在 NQ 和 HotpotQA 上，较基线分别提升 X.X% / X.X%；单跳场景效率提升 X%，多跳场景准确率提升 X%。

2. **引言**
   - RAG 的重要性。
   - 固定策略的问题：单跳过检索、多跳欠检索。
   - 贡献：自适应策略、单跳/多跳对比分析、公开数据集验证。

3. **相关工作**
   - RAG 基础。
   - 检索优化：HyDE、Hybrid Retrieval、Reranker。
   - 自适应 RAG：Self-RAG、CRAG、Query Routing。

4. **方法**
   - 整体框架图。
   - 查询复杂度分类器。
   - 单跳检索策略。
   - 多跳检索策略。
   - 重排序与生成。

5. **实验**
   - 数据集与指标。
   - 实现细节（GPU、模型、参数）。
   - 主实验（NQ + HotpotQA）。
   - 消融实验（单跳/多跳分别报告）。
   - 效率实验。
   - 案例分析。

6. **结论与展望**

## 六、详细时间计划（16—18 天）

| 时间 | 任务 | 产出 |
|---|---|---|
| Day 1—2 | 配置环境：vLLM、sentence-transformers、FAISS、数据集下载 | 环境就绪，数据预处理完成 |
| Day 3—4 | 跑 Baseline（G1），NQ + HotpotQA | Baseline 指标 |
| Day 5—8 | 封装模块：HyDE、Hybrid Retrieval、Reranker，逐组实验 | G2—G5 指标 |
| Day 9—10 | 训练查询复杂度分类器，实现自适应策略（G6） | 微创新模块 + G6 指标 |
| Day 11—12 | 补实验：2WikiMultihopQA 或中文数据集（可选） | 扩展实验 |
| Day 13—14 | 单跳 vs 多跳差异分析，画图，整理表格 | 图表完成 |
| Day 15—16 | 写论文初稿：摘要、方法、实验 | 初稿 |
| Day 17—18 | 修改、降重、补参考文献、调整格式 | 投稿版 |

## 七、几个关键提醒

1. **不要让 LLM 同时占用两张卡**：一张卡跑 LLM，另一张卡跑 Embedding/Reranker/分类器，避免显存争用。
2. **HyDE 生成假设文档时，用同一个 LLM 就行**，但为了效率可以先用小模型生成假设文档。
3. **单跳与多跳的差异要提前分析**：比如 NQ 上 Baseline EM 可能已经 40+，提升有限；HotpotQA 上 Baseline 可能只有 30，提升空间大。这是好现象，可以在论文里写"方法在复杂多跳问题上增益更显著，表明自适应策略能有效应对查询复杂度"。
4. **分类器训练数据要均衡**：从 NQ 和 HotpotQA 各采样等量单跳/多跳问题，否则分类器会偏向单跳。
5. **BM25 索引提前建好**：用 `pyserini` 或 `elasticsearch`，避免每次实验重复建索引。

## 八、一句话总结

> 利用两张 4090 本地部署 LLM、Embedding 与 Reranker，在 NQ（单跳）和 HotpotQA（多跳）上构建一个查询复杂度自适应的 RAG 框架，通过查询扩展、混合检索、重排序和轻量路由器，在单跳上保证效率，在多跳上提升完整性和准确率，最终形成一篇有消融、有差异分析、有自适应创新的 RAG 小论文。